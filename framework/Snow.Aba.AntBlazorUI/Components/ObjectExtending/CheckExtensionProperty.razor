@typeparam TEntity
@typeparam TResourceType
@using Volo.Abp.BlazoriseUI
@using Volo.Abp.Localization

@if (PropertyInfo != null && Entity != null)
{
    <FormItem>
        <Checkbox @bind-Value="@Value">@PropertyInfo.GetLocalizedDisplayName(StringLocalizerFactory)</Checkbox>
    </FormItem>
}